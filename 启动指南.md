# 启动指南 - 解决API连接失败

## 问题：API连接失败

这通常意味着后端服务没有运行或配置不正确。

## 快速解决步骤

### 步骤1：检查后端服务

运行测试脚本：
```bash
python test-backend.py
```

如果显示"连接失败"，说明后端服务没有运行。

### 步骤2：启动后端服务

**在项目根目录（c:\程序\agent）运行：**

```bash
python app.py
```

**应该看到类似输出：**
```
警告: Google Gemini 库导入失败...
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
```

**重要：** 保持这个终端窗口打开，不要关闭！

### 步骤3：验证后端运行

在**另一个终端窗口**运行：
```bash
python test-backend.py
```

应该看到：
```
✓ 所有测试通过！后端服务正常运行
```

### 步骤4：启动前端

**在static-local目录运行：**

```bash
cd static-local
python -m http.server 8000
```

**或者使用启动脚本：**
- 双击 `start-server.bat`

### 步骤5：访问前端

在浏览器中打开：
```
http://localhost:8000
```

## 完整启动流程（两个终端窗口）

### 终端1：后端服务
```bash
cd c:\程序\agent
python app.py
```
**保持运行，不要关闭**

### 终端2：前端服务
```bash
cd c:\程序\agent\static-local
python -m http.server 8000
```
**保持运行，不要关闭**

### 浏览器
访问：`http://localhost:8000`

## 常见问题

### Q: 端口5000被占用
**A:** 修改app.py最后一行：
```python
app.run(debug=False, host='127.0.0.1', port=5001)  # 改为5001
```
然后修改config.js：
```javascript
const API_BASE_URL = 'http://localhost:5001';
```

### Q: 还是连接失败
**A:** 
1. 确认后端服务正在运行（终端1）
2. 确认config.js中的端口号正确
3. 检查防火墙是否阻止了连接
4. 使用诊断工具：`http://127.0.0.1:5000/test-api`

### Q: 如何知道后端是否在运行？
**A:** 运行测试脚本：
```bash
python test-backend.py
```

## 验证清单

- [ ] 后端服务正在运行（终端1显示"Running on..."）
- [ ] 测试脚本显示"所有测试通过"
- [ ] config.js中API_BASE_URL = 'http://localhost:5000'
- [ ] 浏览器能访问 http://127.0.0.1:5000
